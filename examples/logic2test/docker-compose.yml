version: '3.8'

services:
  logic2test:
    build:
      context: ../..
      dockerfile: examples/logic2test/Dockerfile
    volumes:
      - ./input:/input:ro
      - ./out:/out
    command: ["/input/sample_project.c2l.yaml", "-o", "/out/tests", "--type", "all"]
